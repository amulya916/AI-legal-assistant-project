apiVersion: v1
kind: Service
metadata:
  name: ailegalproject-service
  labels:
    app: ailegalproject
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
  selector:
    app: ailegalproject
